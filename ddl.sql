create database toDo;

use toDo;

create table tasks(
    id int auto_increment not null primary key,
    tarefa varchar (85) not null,
    descricao text,
    responsavel varchar (85) not
);